export const environment = {
  /**
   * Base URL for the .NET SQL benchmark API.
   *
   * In production, this is typically left as an empty string and the app
   * calls the API on the same origin (via reverse proxy or gateway).
   */
  apiBaseUrl: '',
};


export const environment = {
  /**
   * Empty string — requests go through the Angular dev server proxy (proxy.conf.json),
   * which forwards /health and /scenarios to http://localhost:30080.
   * This avoids CORS issues during local development.
   */
  apiBaseUrl: '',
};

